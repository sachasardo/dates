<nav class="navbar navbar-default navbar-wagon" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand padding-left" href="/">
        <%= image_tag "logo.png", class:"img" %>
      </a>
      <div class="logo">
      <p>Dates</p>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
             <%= link_to "", _path, class: "btn btn-success"  %>

          </li>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <% if current_user.picture_file_size == nil %>
                 <%= image_tag 'default_user_image.png', class: "img-avatar" %>
              <% else %>
              <div class="img-avatar" style='background-image:url(<%= image_path current_user.picture, class: "img-avatar" %>)'>
              </div>
              <% end %>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to user_path(current_user) do %>
                  <i class="fa fa-user"></i> <%= t ".profile", default: "Profile" %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Sign out" %>
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
            <li>
              <%= link_to "Juge Arbitre ?", judge_path %>
            </li>
            <li>
              <%= link_to t(".sign_up", default: "Inscription"), new_user_registration_path, id:"nav", class: "btn btn-success" %>
            </li>
            <li>
              <%= link_to t(".sign_in", default: "Connexion"), new_user_session_path, id:"nav", class: "btn btn-success"  %>
            </li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
